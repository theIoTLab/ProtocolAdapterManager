<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.fistar.sdcs.pa" >

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="eu.fistar.sdcs.pa.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PAManagerService"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action
                    android:name="eu.fistar.sdcs.pa" />
            </intent-filter>
        </service>

        <service
            android:name=".da.bthdp.HDPDeviceAdapter"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BLUETOOTH">
            <intent-filter>
                <action
                    android:name="eu.fistar.sdcs.pa.da.bthdp.HDPDeviceAdapter" />
            </intent-filter>
        </service>

        <service
            android:name=".da.bthdp.hdpservice.HDPHealthManagerService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:permission="android.permission.BLUETOOTH">
            <intent-filter>
                <action
                    android:name="eu.fistar.sdcs.pa.da.bthdp.hdpservice.HDPHealthManagerService" />
            </intent-filter>
        </service>

        <service
            android:name=".da.bthdp.hdpservice.HDPDriverService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BLUETOOTH" />

    </application>

</manifest>
